// src/types/widget.ts — barrel re-export (zero breaking changes)
// Los imports existentes como `import { Widget } from '@/types/widget'` siguen funcionando sin cambios
export * from './widgets/base';
export * from './widgets/configs';
export * from './widgets/metadata';
